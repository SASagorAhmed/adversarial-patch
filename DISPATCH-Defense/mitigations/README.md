# mitigations/

Real DISPATCH mitigation runs live here using the same overall style as previous
mitigation experiments:

```text
mitigation_01/
mitigation_02/
...
```

Validation / smoke tests must NEVER create folders here.

Each mitigation is self-contained. Never overwrite an existing mitigation folder.
