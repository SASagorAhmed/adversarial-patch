# DISPATCH models

Official CompVis LDM inpainting checkpoint lives with the third-party repo:

`third_party/latent-diffusion/models/ldm/inpainting_big/last.ckpt`

Do not place alternate diffusion models here silently.

See `checkpoint_manifest.txt` (created after download) for SHA256 identity.
